# Markdown conformance corpus

Vendored, offline CommonMark and GFM reference examples used by the
`markdown-conformance-suite` (see `MarkdownConformance*` tests). Each file is an
array of records in the CommonMark reference shape:

```json
{ "markdown": "...", "html": "...", "example": 1, "section": "ATX headings" }
```

`markdown` is the source, `html` is the upstream reference output, and `section`
is the construct category the compatibility matrix maps to a support tier.

## Provenance (pinned)

| File | Upstream source | Version | Licence |
| --- | --- | --- | --- |
| `commonmark-0.31.2.spec.json` | <https://spec.commonmark.org/0.31.2/spec.json> | CommonMark spec **0.31.2** | CC-BY-SA 4.0 |
| `gfm-0.29.extensions.spec.json` | <https://raw.githubusercontent.com/github/cmark-gfm/0.29.0.gfm.13/test/spec.txt> | GFM spec **0.29** (tag `0.29.0.gfm.13`) | CC-BY-SA 4.0 |

Retrieved 2026-06-26.

- **CommonMark** is vendored verbatim (652 examples, 26 sections).
- **GFM** is the extension-only subset (24 examples, 5 sections: Tables, Task
  list items, Strikethrough, Autolinks, Disallowed Raw HTML). GFM's `spec.txt`
  is a superset of CommonMark; only sections whose name ends with `(extension)`
  are kept so the corpus does not duplicate CommonMark. It is regenerated by
  `Scripts/extract_gfm_corpus.py` (which also normalises the spec's `→` tab
  markers back to real tabs).

## Updating the corpus

Bump the pinned version, re-download, and re-run the extractor. The conformance
baseline (`Baseline/*.json`) and the compatibility matrix are expected to fail
until any new or renamed `section` is classified — that is the intended signal,
not a bug.
